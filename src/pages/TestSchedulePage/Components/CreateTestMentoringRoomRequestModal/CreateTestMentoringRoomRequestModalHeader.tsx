import { Typography } from "@mui/material";

export const CreateTestMentoringRoomRequestModalHeader = (): JSX.Element => {
  return (
    <Typography variant="subtitle1" sx={{ fontWeight: "bolder", mb: 2 }}>
      질의응답방 생성신청
    </Typography>
  );
};

export default CreateTestMentoringRoomRequestModalHeader;
